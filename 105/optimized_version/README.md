# Optimized Version

This folder contains the optimized sari-sari store system.

It includes the full system plus the Analytics/DSA page using:

- Hash table lookup
- Binary search
- Merge sort
- Min-heap restock queue
- Dijkstra supplier optimizer

Run with:

```bash
python main.py
```

Benchmark:

```bash
python benchmark.py
```
