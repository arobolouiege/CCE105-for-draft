# Baseline Version

This folder contains the baseline sari-sari store system.

It includes the regular inventory, stock-in, supplier, low-stock, and dashboard pages.
The optimized Analytics/DSA page is intentionally removed from this version.

Run with:

```bash
python main.py
```

Benchmark:

```bash
python benchmark.py
```
